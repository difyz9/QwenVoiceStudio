# Qwen Voice Studio 对比 qwen3-TTS-studio 的优化方案

## 1. 对比对象

- 当前项目：Qwen Voice Studio
- 参考项目：bc-dunia/qwen3-TTS-studio
- 参考仓库：https://github.com/bc-dunia/qwen3-TTS-studio

本文目标不是做功能清单复述，而是给出一份可直接进入排期的优化方案。

## 2. 结论摘要

两个项目的方向并不完全相同。

Qwen Voice Studio 更像一个面向业务流程的语音后台，已经具备：

- 登录与管理台
- 预置音色管理
- 参考音频生成与状态持久化
- 批量语音合成
- 资产目录沉淀
- Docker Compose 部署

qwen3-TTS-studio 更像一个面向创作者和高级用户的单机工作台，强项在于：

- 模型能力覆盖更广
- 参数调优能力更强
- Preview 与历史能力更成熟
- Persona / Podcast 工作流更完整
- 交互反馈更细粒度

一句话评价：

- 我们的项目更适合继续演进成团队协作型语音生产后台
- 对方项目更适合作为“高级 TTS 创作工作站”的能力标杆

## 3. 当前项目的优势

### 3.1 架构更适合做正式产品

当前项目使用：

- Next.js 前端
- FastAPI 后端
- PostgreSQL 持久化
- Docker Compose 部署

这比单文件 Gradio 应用更适合继续扩展：

- 权限与会话
- 审计与日志
- 任务中心
- 多用户协作
- 后续 SaaS 化

### 3.2 preset 生命周期已经成型

当前项目已经把音色管理做成了完整资产链：

- 文案描述
- 参考文本
- 生成参考音频
- 写入资产目录
- 进入合成流程复用

这是非常好的产品基础。

### 3.3 状态持久化已经起步

目前已经有：

- `reference_audio_status`
- `reference_audio_error`

这意味着后续做统一任务系统、队列、重试、进度条的成本会比从零开始低很多。

## 4. 当前项目相对参考项目的差距

### 4.1 模型交互层能力不足

相对 `qwen3-TTS-studio`，当前项目还缺：

- 参数调优入口：temperature、top_k、top_p、repetition_penalty 等
- 质量档位预设：Fast / Balanced / Quality
- 多模型切换：0.6B / 1.7B / Base / VoiceDesign 的显式选择
- 更细的语言与风格控制

影响：

- 对高级用户吸引力不够
- 结果不可控时缺少调参手段

### 4.2 音色来源能力单一

当前项目主要围绕：

- VoiceDesign 生成参考音频
- Base 模型复用生成

缺少：

- 上传参考音频进行 voice clone
- 多样本 clone
- 自动转写或 transcript 辅助
- 保存自定义声音资产

影响：

- 只能做“根据文案设计声音”
- 不能覆盖更常见的“我有样音，帮我复刻”的需求

### 4.3 任务系统仍然过轻

虽然已经持久化了状态，但目前真正执行生成的仍然是 API 进程内任务。

问题包括：

- API 进程内推理容易阻塞
- OOM 会把接口进程一起带崩
- 没有独立 worker
- 缺少统一任务表
- 缺少开始时间、结束时间、重试次数、运行耗时

影响：

- 一旦任务变多，稳定性会很快成为瓶颈

### 4.4 前端工作流深度不足

相对参考项目，当前前端还缺：

- 历史中心
- 任务详情页
- 音色卡片的完整状态面板
- 快速 preview 机制
- 参数 preset
- 更明显的任务反馈与进度展示
- 失败重试策略和异常解释

影响：

- 能用，但还不够像成熟工具

### 4.5 开源工程化包装偏弱

相对参考项目，当前仓库还缺：

- LICENSE
- CI
- smoke test
- CONTRIBUTING
- issue / PR 模板
- 预构建镜像发布策略
- 更清晰的 release 说明

影响：

- 对外部贡献者不够友好
- 对开源用户的“开箱信任感”不足

## 5. 优化目标

建议把目标分成三层。

### 5.1 P0：稳定跑通

解决“能不能稳定生成、能不能稳定恢复、能不能稳定部署”的问题。

### 5.2 P1：产品闭环

解决“用户是否能顺畅完成从建音色到试听到合成到回看结果”的问题。

### 5.3 P2：能力扩展

解决“我们是否能覆盖更广的 TTS 创作场景”的问题。

## 6. 优化方案

## 6.1 P0 稳定性优化

### 方案 1：把生成任务迁移到独立 worker

建议内容：

- 引入独立任务执行进程
- API 只负责创建任务和读状态
- worker 负责加载模型和执行推理

推荐实现顺序：

1. 先做单 worker 进程
2. 再接任务队列
3. 最后考虑并发和优先级

可选技术：

- Celery + Redis
- RQ + Redis
- Arq
- 轻量自研数据库轮询 worker

优先建议：

- 如果追求快落地：RQ 或简单 worker
- 如果追求长期扩展：Celery 或 Arq

验收标准：

- API 进程不再直接执行重型推理
- 服务重启不会让执行中的任务无迹可寻
- 任务状态和错误信息可追踪

### 方案 2：建立统一任务模型

建议增加独立任务表，而不是把状态散落在 preset 或 synthesis 记录里。

建议字段：

- `job_code`
- `job_type`
- `target_type`
- `target_id`
- `status`
- `progress_step`
- `progress_percent`
- `started_at`
- `finished_at`
- `error_message`
- `retry_count`
- `worker_id`

价值：

- preset 生成和 synthesis job 可以统一进入任务中心
- 前端能够展示统一历史和进度

### 方案 3：为任务加恢复机制

建议内容：

- 为 `generating/running` 任务设置超时回收
- 对服务中断导致的任务，标记为 `failed` 或 `stale`
- 支持明确重试入口

验收标准：

- 不会出现任务永久卡死在运行中

### 方案 4：统一模型路径与资源策略

建议内容：

- 所有模型优先走本地挂载目录
- Base 模型也允许本地路径配置
- 启动时检查模型是否存在
- 启动时输出明确的模型解析结果

可补充功能：

- 启动前健康检查
- 模型目录校验接口

## 6.2 P1 产品闭环优化

### 方案 5：增加任务中心页面

页面目标：

- 汇总所有参考音频生成任务
- 汇总所有合成任务
- 展示状态、耗时、错误、输出路径

建议字段：

- 类型：参考音频生成 / 合成
- 目标：preset_code / job_code
- 状态
- 创建时间
- 开始时间
- 完成时间
- 最近错误
- 操作：重试 / 查看详情 / 打开结果

价值：

- 当前页面分散，缺少统一任务视角

### 方案 6：补完整的音色卡片工作流

当前音色库已经有基础操作，但还可以继续升级成“工作台卡片”。

建议卡片展示：

- preset 名称
- 标签：builtin / designed / cloned
- 当前状态：missing / generating / failed / ready
- 最近一次成功生成时间
- 失败原因摘要
- 参考文本摘要
- 操作按钮：试听 / 生成 / 重试 / 去合成 / 查看详情

价值：

- 减少用户在不同页面之间切换

### 方案 7：增加历史中心

建议内容：

- 保存音色设计记录
- 保存合成记录
- 支持按时间、状态、preset 搜索
- 支持直接回放和跳转复用

这是参考项目在体验上非常领先的一块。

### 方案 8：增加参数预设

建议先不把所有高级参数都暴露出来，而是先做：

- Fast
- Balanced
- Quality

后续再提供高级参数面板。

好处：

- 降低新手门槛
- 保留专业扩展空间

### 方案 9：更好的进行中反馈

建议内容：

- 开始时间
- 已耗时
- 预计剩余时间
- 当前阶段：加载模型 / 生成音频 / 写文件 / 完成

即使短期做不到精确百分比，也应先做阶段型进度。

## 6.3 P2 能力扩展优化

### 方案 10：支持 voice clone 模式

建议能力：

- 上传 1 到 3 个参考音频样本
- 手工填写 transcript 或自动转写
- 生成 clone prompt
- 保存为新 voice 资产

这是参考项目最值得借鉴的一块。

### 方案 11：支持多样本 clone 分析

建议内容：

- 样本时长检查
- 文件格式检查
- 采样率标准化
- 简单质量提示

即使先不做复杂 SNR，也应该先做基础检查。

### 方案 12：引入 persona 抽象

建议把 preset 升级为更强的角色资产，增加：

- 描述
- 场景
- 标签
- 默认参数
- 角色备注

后续可以自然扩展到：

- 多说话人内容生成
- 剧本生成
- 播客或对话式音频

### 方案 13：支持多模型切换

建议至少支持：

- VoiceDesign
- Base
- 轻量模型

并让用户在速度和质量之间做选择。

## 7. 开源仓库层面的优化建议

### 7.1 补齐开源基础文件

建议增加：

- `LICENSE`
- `CONTRIBUTING.md`
- `CODE_OF_CONDUCT.md`
- `SECURITY.md`

### 7.2 增加 CI

建议最小工作流：

- 前端 typecheck
- 前端 build
- 后端 compileall
- docker compose config

如果再进一步：

- 增加 smoke test

### 7.3 增加 Docker smoke test

建议参考项目做法，增加一个容器内最小生成验证。

目标：

- 镜像能启动
- 模型路径正常
- 能写出 wav 文件
- 输出文件非空

### 7.4 增加发布策略

建议：

- GitHub Release
- 版本号管理
- 预构建镜像（GHCR）

## 8. 推荐排期

## 阶段一：1 到 2 周

目标：把系统变得更稳定。

建议任务：

1. 独立 worker 原型
2. 统一任务表
3. synthesis 任务也接入统一状态
4. 任务中心初版
5. Base 模型本地路径配置

输出结果：

- 任务不再依赖 API 进程
- 状态更完整
- 系统能长期跑而不是只演示

## 阶段二：2 到 4 周

目标：补齐业务闭环体验。

建议任务：

1. 音色卡片升级
2. 历史中心
3. 参数预设
4. 更好的进度反馈
5. 合成任务详情页

输出结果：

- 用户可从音色设计一路走到合成与回看

## 阶段三：4 到 8 周

目标：扩大能力边界。

建议任务：

1. voice clone 上传模式
2. 多样本 clone
3. persona 抽象
4. 多模型支持
5. 开源工程化完善

输出结果：

- 从“后台工具”升级成“开源语音工作台平台”

## 9. 优先级结论

如果只能选最重要的三件事，建议优先做：

1. 独立任务 worker + 统一任务表
2. 任务中心 + 更完整状态反馈
3. voice clone 模式

原因很直接：

- 第 1 项决定系统能不能稳定
- 第 2 项决定产品是否易用
- 第 3 项决定能力边界是否真正接近参考项目

## 10. 最终判断

当前项目不适合简单照着 `qwen3-TTS-studio` 做功能复制。

更合理的策略是：

- 保持我们“后台系统”的架构优势
- 有选择地吸收对方在 preview、history、persona、参数 preset、voice clone 上的强项

也就是说，不是把项目做成另一个 Gradio Studio，而是把它做成：

一个更稳定、更可管理、更适合业务生产的 Qwen TTS 工作台。
